rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo mode — any authenticated user (including anonymous) can read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
